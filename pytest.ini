[pytest]
testpaths = test
python_files = test_*.py *_unit.py *_smoke.py
addopts = -v --tb=short
markers =
    unit: unit tests
    smoke: smoke tests
    integration: needs real DB env vars (skip in CI by default)
